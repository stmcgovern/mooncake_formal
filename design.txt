Mooncake KV cache disaggregated serving

What we formalize and why:

  Mooncake disaggregates KV cache from GPU compute in LLM serving.
  Prefill nodes produce KV blocks; these are stored in a distributed
  pool (DRAM, SSD, CXL); decode nodes fetch them on demand via RDMA.
  The core insight: trade cheap storage for expensive GPU recomputation.

  Strip away the transport layer and the scheduler — what remains is a
  distributed object store with a lifecycle protocol.  The objects are
  KV cache blocks.  The lifecycle is: produce → transfer → store →
  serve → evict.  The store manages two interlocking state machines:
  replica status (per-object) and segment status (per-container).

  The core correctness question: do the protocol gates compose safely?
  Each gate is trivial alone — readable iff COMPLETE, allocatable iff
  SegOk, protected iff pinned or referenced.  But their COMPOSITION
  has subtle interleavings that testing can't enumerate: can you drain
  a segment while a transfer is writing to a replica on it?  Can you
  evict an object that another transfer is about to read?

  We prove: (1) each state machine has no self-transitions and terminal
  states are absorbing, (2) eviction protection is coherent (pinned OR
  referenced OR in-progress → protected), (3) segment operations respect
  status gates, (4) transfer slice counting is monotonic, (5) the
  protocol gates compose into a single global invariant.

What we model and what we leave out:

  MODEL (high verification value):
  - Replica status state machine (INITIALIZED→PROCESSING→COMPLETE→REMOVED, +FAILED)
  - Segment status lifecycle (OK→DRAINING→DRAINED→UNMOUNTING)
  - Eviction protection (pin levels, ref counting, status gates)
  - Transfer task lifecycle (PENDING→ACTIVE→COMPLETED/FAILED)
  - Slice counting invariant (done_slices ≤ total_slices, monotonic)
  - Segment capacity bookkeeping (used ≤ capacity)
  - Object metadata and placement safety (allocate only on SegOk)
  - Drain/unmount preconditions (empty + drained before unmount)

  LEAVE OUT (low verification value or too coupled to internals):
  - RDMA/NVLink transport layer details (hardware-specific, not protocol-level)
  - Allocator implementations (CachelibBuffer, OffsetBuffer — memory management internals)
  - Standby/HA state machine (STOPPED→CONNECTING→...→PROMOTED — complex but orthogonal)
  - OpLog replication and snapshot mechanics (persistence layer, not core protocol)
  - P2P store shard distribution (Go codebase, etcd metadata — different language/paradigm)
  - EP (expert parallelism) — separate concern, formalized in deepep_formal
  - File storage backend, io_uring — I/O implementation details
  - Eviction strategy implementations (LRU/FIFO ordering — algorithm, not safety)
  - Lease TTL renewal mechanics (timing-dependent, hard to model usefully)
  - IBGDA/RoCE queue pair setup (hardware initialization)

MODELING DECISIONS
==================

Non-obvious choices that affect multiple files.

  1. Replica status without UNDEFINED: Source has 6 states (UNDEFINED,
     INITIALIZED, PROCESSING, COMPLETE, FAILED, REMOVED).  We drop
     UNDEFINED — it represents uninitialized memory, not a protocol
     state.  A replica enters our model at INITIALIZED.
     WHY: UNDEFINED has no meaningful transitions and adds noise to
     every exhaustive case analysis.

  2. Weights as nat, not floats: Eviction priority, transfer progress,
     and object sizes are all nat.  We don't model floating-point
     watermark ratios (e.g. high_watermark_ratio=0.95).
     WHY: The safety properties (eviction protection, capacity bounds)
     are structural, not numerical.  Nat arithmetic keeps proofs
     decidable.

  3. Pin levels as a 3-element type, not nat: Source uses implicit
     priority; we make it explicit with Unpinned < SoftPinned < HardPinned
     and a decidable ordering.
     WHY: Makes eviction protection predicates compositional — we can
     state "hard-pinned implies protected" as a standalone lemma.

  4. Transfer slices as counts, not buffers: Source tracks actual data
     buffers per slice with variant descriptors (RDMA, memcpy, file).
     We track only (done_slices, total_slices) counts.
     WHY: The safety property is monotonicity (done ≤ total, done
     increases), not data content.  Buffer types are transport-layer
     concerns we exclude.

  5. Segment status without UNDEFINED: Same rationale as replica — a
     segment enters our model at SegOk (mounted and available).
     WHY: Keeps segment_transition inductive clean.

  6. Eviction protection is a bool predicate, not a lock: Source uses
     atomic refcnt + weak_ptr + scoped access.  We model the logical
     predicate: protected iff hard-pinned OR ref_count > 0 OR not-yet-
     readable.
     WHY: The invariant is "protected objects are not evicted", not
     "the lock is held correctly".  The latter is a concurrency concern
     we don't model.

  7. LTS labels are 5 operations, not 9: The initial plan called for 9
     labels (including transfer operations).  We use 5: LPlace, LRemove,
     LBeginDrain, LCompleteDrain, LUnmount.  Transfer operations are
     excluded from the LTS because the store_step relation operates on
     StoreState (segment + transfers), and transfer status changes don't
     interact with segment operations in the current model.
     WHY: Keeping the LTS minimal simplifies case analysis in every
     theorem.  Transfer properties are proved separately in
     TransferProtocol.v.

  8. Drain liveness uses object_count as Lyapunov function, not
     composed_progress_sound: The plan called for instantiating DML's
     composed progress framework.  Instead, we use a simpler argument:
     object_count is bounded below by 0 and never increases on draining
     segments, so the drain phase terminates in at most object_count steps.
     WHY: The composed progress framework requires an active/frame
     decomposition with explicit per-component indices.  The Lyapunov
     argument is self-contained and sufficient for drain termination.

  10. Enriched LTS as a separate relation: The read/evict operations
     are defined in a separate rich_store_step relation rather than
     extending store_step.  This avoids breaking all existing proofs
     (which would need more cases) while still enabling the safety
     theorems.
     WHY: Extending store_step with 3 new constructors would require
     updating every proof that does case analysis on store_step
     (store_step_preserves_consistency, drain_never_adds_objects, etc.).
     The enriched LTS duplicates the 5 original constructors but keeps
     existing theorems intact.

  12. Frame properties via label_targets classification: rather than proving
     per-step frame lemmas individually (one per constructor), we define
     label_targets : RichStoreLabel → ObjectId → bool and prove one general
     theorem (non_targeting_preserves_find) that covers all 8 constructors.
     WHY: Reduces 8 per-step lemmas to 1 master theorem.  New constructors
     added to the enriched LTS only need one new case in the master proof.
     The specific concurrent safety theorems are corollaries.

  11. Evict vs Remove: The existing LRemove is unconditional (used during
     drain cleanup).  RLEvict adds preconditions (eviction_protected =
     false).  In the real system, eviction and drain-cleanup are distinct
     code paths with different safety checks.
     WHY: The distinction captures the real protocol: eviction is guarded
     by refcount/pin/status checks, while drain-time removal is forced
     (the segment is being reclaimed regardless).

  9. drain_wf (well-foundedness of drain_step) is not provable: The
     drain_step relation wraps LRemove, but remove_object is always
     enabled (it's a filter that may be a no-op).  A no-op removal
     doesn't decrease any measure, so rank_wf cannot be instantiated.
     WHY: Well-foundedness requires every step to make progress, but
     removal of a non-existent object is a valid step that does nothing.
     The bounded theorem (drain_bounded) captures the useful property
     without requiring well-foundedness.

HEADLINE THEOREMS
=================

  Layer 1 — State machines (Core.v):
  1. replica_no_self_transition — no replica status is its own successor
  2. segment_no_self_transition — no segment status is its own successor
  3. no_transition_from_removed — REMOVED is terminal for replicas
  4. no_transition_from_unmounting — UNMOUNTING is terminal for segments
  5. processing_not_evictable — in-progress writes are always protected
  6. hard_pinned_protected — hard-pinned objects are always protected
  7. active_readers_protected — objects with ref_count > 0 are protected
  8. readable_is_complete — readability implies COMPLETE status

  Layer 2 — Segment operations (SegmentManager.v):
  9. place_requires_ok — placement requires SegOk segment
  10. empty_drained_safe — empty + drained = safe to unmount

  Layer 2 — Transfer protocol (TransferProtocol.v):
  11. create_task_slice_inv — new tasks satisfy slice invariant
  12. complete_slice_preserves_inv — slice completion is monotonic
  13. transfer_no_self_transition — no transfer status loops
  14. task_finished_iff — task_finished reflects terminal status

  Layer 2 — Unified LTS (StoreLTS.v):
  15. store_step_preserves_consistency — every store_step preserves store_consistent
  16. store_consistent_star — store_consistent through arbitrary step sequences (via DML lstar)

  Layer 2-3 — Acyclicity and well-foundedness (Acyclicity.v):
  17. replica_acyclic — no replica status reaches itself through any path (subsumes #1)
  18. segment_acyclic — no segment status reaches itself through any path (subsumes #2)
  19. transfer_acyclic — no transfer status reaches itself through any path
  20. replica_wf, segment_wf, transfer_wf — all component transitions are well-founded
  21. progress_step_rank_nonincreasing — progress steps never increase composed rank
  22. drain_step_decreases_rank — drain operations strictly decrease composed rank

  Layer 3 — Drain protocol (DrainProtocol.v):
  23. drain_sequence_valid — Ok→Draining→Drained→Unmounting is valid
  24. drain_sequence_blocks_placement — no placement at any drain stage
  25. drain_then_unmount_safe — empty segment: drain + complete = safe to unmount
  26. drainable_no_processing — drainable segments have no in-progress transfers

  Layer 3 — Transfer-replica bridge (ReplicaTransfer.v):
  27. transfer_success_valid_transition — task succeeded → PROCESSING→COMPLETE valid
  28. transfer_failure_valid_transition — task failed → PROCESSING→FAILED valid
  29. removed_not_coupled_active — REMOVED replicas can't have active transfers

  Layer 3 — Refinement morphisms (StoreRefinement.v):
  30. segment_invariant_lift — component invariants lift to composed store (via DML Refinement)
  31. segment_trace_projection — store traces project to valid segment traces
  32. once_draining_never_ok — draining segment never returns to SegOk
  33. once_draining_no_placement — once draining, placement is always blocked
  34. segment_rank_monotone — segment rank never increases through store traces

  Layer 4 — Composition (StoreComposition.v):
  35. four_gates_hold — all four protocol gates hold simultaneously on consistent store
  36. evictable_characterization — evictable ↔ complete ∧ not hard-pinned ∧ no readers
  37. processing_only_on_ok — PROCESSING objects exist only on SegOk segments
  38. place_preserves_consistency — placing an object preserves store consistency
  39. remove_preserves_consistency — removing an object preserves store consistency
  40. begin_drain_preserves_consistency — starting drain preserves store consistency
  41. no_incomplete_reads — no reads from non-COMPLETE replicas
  42. no_unmount_with_objects — unmount requires empty segment

  Layer 4 — Drain liveness (DrainProgress.v):
  43. drain_never_adds_objects — object count never increases on non-allocatable segments
  44. remove_present_decreases — removing a present object strictly decreases count
  45. drain_completion_enabled — empty draining segment enables CompleteDrain
  46. drain_trace_monotone — object count never increases through removal traces
  47. drain_invariant — draining status and consistency preserved through removal traces
  48. drain_bounded — drain phase bounded by initial object count; emptiness enables completion

  Layer 4 — Lifecycle convergence (Convergence.v):
  49. filter_comm — filter operations commute (order-independence of removals)
  50. remove_objects_comm — removal order doesn't affect resulting objects list
  51. sequential_remove_lstar — removing all objects one by one empties the segment
  52. lifecycle_convergence — HEADLINE: segment reaches SegUnmounting in exactly n+3 steps
  53. lifecycle_trace_length — tight bound: trace length = object_count + 3
  54. segment_reclaimable — every segment with unique IDs is reclaimable

  Layer 2-3 — Refcount protocol (RefcountProtocol.v):
  55. inc_protects — incrementing refcount makes object eviction-protected
  56. evictable_zero_refcnt — evictable implies refcount = 0
  57. evictable_is_complete — evictable implies status = COMPLETE
  58. inc_segment_refcnt_protects — inc on segment produces protected object
  59. find_after_update — object lookup after update_object_meta gives f(original)
  60. update_preserves_live — refcount changes preserve object liveness

  Layer 4 — Eviction safety (EvictionSafety.v):
  61. read_eviction_safety — HEADLINE: refcnt > 0 prevents eviction
  62. begin_read_blocks_eviction — HEADLINE: BeginRead makes Evict impossible
  63. evict_is_guarded_remove — Evict is a precondition-checked Remove
  64. triple_protection — pin, refcount, or non-readable each independently protect
  65. eviction_requires_all_absent — eviction requires all three protections absent

  Layer 3-4 — Protection frame properties (ProtectionFrame.v):
  66. find_in_filter_neq — object lookup after filtering a different ID is preserved
  67. find_after_update_neq — object lookup after updating a different ID is preserved
  68. non_targeting_preserves_find — GENERAL: any step not targeting an object preserves it
  69. concurrent_eviction_safe — HEADLINE: evicting B doesn't affect A's read protection
  70. concurrent_reads_independent — HEADLINE: reading B doesn't interfere with A's protection
  71. drain_preserves_read_protection — HEADLINE: removing B during drain preserves A's read protection
  72. end_read_preserves_other_protection — ending a read on B preserves A's protection
  73. trace_non_targeting_preserves_find — multi-step frame: find preserved through traces
  74. trace_read_protection — HEADLINE: active readers protected through arbitrary traces

  Layer 3-4 — Object lifecycle completeness (ObjectLifecycle.v):
  75. find_in_filter_self — filtering by ID removes all matches
  76. remove_makes_unfindable — remove_object makes target unfindable
  77. eviction_makes_unfindable — Evict step removes the object
  78. place_makes_findable — Place step makes new object findable
  79. eviction_possible — unprotected + findable → eviction step exists
  80. read_possible — COMPLETE + findable → BeginRead step exists
  81. read_or_evict — HEADLINE: protection dichotomy (protected xor evictable)
  82. active_readers_block_eviction — active readers → protected ∧ unevictable
  83. begin_read_protects — BeginRead changes protection and increments refcount
  84. evict_and_remove_same_effect — eviction and removal have same state effect

  -- Diamond property (independent operations commute) --
  85. filter_map_comm_neq — filter and map on different IDs commute (list level)
  86. map_map_comm_neq — maps on different IDs commute (list level)
  87. freed_filter_independent — freed-space unaffected by filtering other ID
  88. freed_map_independent — freed-space unaffected by mapping other ID
  89. remove_update_seg_comm — remove and update commute at segment level
  90. update_update_seg_comm — updates on different IDs commute at segment level
  91. remove_remove_seg_comm — removes on different IDs commute (full equality)
  92. concurrent_reads_diamond — HEADLINE: BeginRead(A);BeginRead(B) = BeginRead(B);BeginRead(A)
  93. evict_read_diamond — HEADLINE: Evict(A);BeginRead(B) = BeginRead(B);Evict(A)
  94. remove_remove_diamond — HEADLINE: Remove(A);Remove(B) = Remove(B);Remove(A)
  95. evict_evict_diamond — HEADLINE: Evict(A);Evict(B) = Evict(B);Evict(A)
  96. remove_read_diamond — HEADLINE: Remove(A);BeginRead(B) = BeginRead(B);Remove(A)
  97. end_read_end_read_diamond — HEADLINE: EndRead(A);EndRead(B) = EndRead(B);EndRead(A)
  98. begin_read_end_read_diamond — HEADLINE: BeginRead(A);EndRead(B) = EndRead(B);BeginRead(A)
  99. evict_end_read_diamond — HEADLINE: Evict(A);EndRead(B) = EndRead(B);Evict(A)
  100. remove_end_read_diamond — HEADLINE: Remove(A);EndRead(B) = EndRead(B);Remove(A)

  -- General diamond (capstone) --
  101. general_object_diamond — HEADLINE: ALL object ops on different IDs commute

  -- Status-object diamonds (segment lifecycle vs object operations) --
  102. drain_read_diamond — BeginDrain;BeginRead = BeginRead;BeginDrain
  103. drain_end_read_diamond — BeginDrain;EndRead = EndRead;BeginDrain
  104. drain_evict_diamond — BeginDrain;Evict = Evict;BeginDrain
  105. drain_remove_diamond — BeginDrain;Remove = Remove;BeginDrain

  -- Read session transparency --
  106. dec_refcnt_inc_refcnt — dec(inc(m)) = m (object-level cancellation)
  107. read_session_transparent — HEADLINE: BeginRead;EndRead = identity (segment level)
  108. read_session_store_transparent — HEADLINE: BeginRead;EndRead = identity (store level)

  -- NoDup preservation (unique object IDs) --
  109. nodup_filter_preserved — filtering preserves NoDup
  110. nodup_map_id_preserved — id-preserving map preserves NoDup
  111. nodup_remove_preserved — remove_object preserves NoDup
  112. nodup_update_preserved — update_object_meta preserves NoDup
  113. nodup_step_preserved — HEADLINE: every enriched step preserves NoDup

  -- Trace equivalence (Mazurkiewicz trace monoid) --
  114. rich_store_step_deterministic — single-step determinism of the enriched LTS
  115. exec_deterministic — multi-step determinism: same trace → same result
  116. exec_split — split execution at concatenation point
  117. exec_join — join execution of concatenated lists
  118. independent_sym — label independence is symmetric
  119. independent_diamond — HEADLINE: unified diamond for all independent ops
  120. swap_preserves_result — HEADLINE: adjacent swap of independent ops preserves final state

  -- Refcount conservation law (quantitative trace invariant) --
  121. find_after_update_seg — segment-level lookup after update gives f(original)
  122. begin_read_find — BeginRead makes find_object return inc_refcnt(m)
  123. end_read_find — EndRead makes find_object return dec_refcnt(m)
  124. refcount_counting — HEADLINE: refcount_final + #EndRead = refcount_initial + #BeginRead
  125. protection_from_counting — HEADLINE: excess BeginReads over EndReads imply eviction protection

  -- Denotational semantics and adequacy --
  126. no_in_progress_reflect — decidable no_in_progress (boolean <-> Prop)
  127. safe_to_unmount_reflect — decidable safe_to_unmount (boolean <-> Prop)
  128. step_eval_sound — step_eval Some -> rich_store_step
  129. step_eval_complete — rich_store_step -> step_eval Some
  130. eval_sound — eval Some -> exec (multi-step soundness)
  131. eval_complete — exec -> eval Some (multi-step completeness)
  132. eval_adequacy — HEADLINE: eval ls st = Some st' <-> exec st ls st'
  133. eval_app — HEADLINE: eval distributes over trace concatenation (monoid homomorphism)
  134. eval_app_some — concatenation of successful evaluations succeeds
  135. eval_app_none — failure in first half propagates through concatenation

  -- Drain safety composition (lifecycle + frame + protection) --
  136. exec_to_lstar — bridge exec -> lstar rich_store_step
  137. lstar_to_exec — bridge lstar rich_store_step -> exec
  138. remove_labels_non_targeting — removal trace doesn't target absent IDs
  139. removal_trace_preserves_find — multi-step removal preserves non-targeted find
  140. nodup_removal_trace — NoDup preserved through removal traces
  141. drain_phase_safety — HEADLINE: BeginDrain + selective removal preserves readers
  142. drain_phase_eval — HEADLINE: denotational drain preserves readers
  143. full_drain_safety — HEADLINE: any non-targeting trace preserves readers
  144. nodup_non_targeting_trace — NoDup preserved through non-Place traces
  145. status_labels_non_targeting — status ops don't target any object

  -- Enriched consistency (store_consistent through all 8 operations) --
  146. meta_update_preserves_consistency — status-preserving metadata updates preserve store_consistent
  147. rich_step_preserves_consistency — HEADLINE: every enriched step preserves store_consistent
  148. rich_consistent_exec — HEADLINE: store_consistent through arbitrary enriched traces
  149. eval_preserves_consistency — HEADLINE: eval preserves store_consistent

  -- Capacity coherence (resource accounting invariant) --
  150. sum_sizes_partition — CORE: total size = kept + freed (partition identity)
  151. freed_eq_sum_matching — freed computation = sum of matching elements
  152. place_preserves_coherence — Place preserves seg_used = sum_sizes
  153. remove_preserves_coherence — Remove preserves seg_used = sum_sizes
  154. update_preserves_coherence — metadata updates preserve coherence (when om_size preserved)
  155. step_preserves_coherence — HEADLINE: every enriched step preserves capacity coherence
  156. place_preserves_wf — has_room → place preserves used ≤ capacity
  157. remove_preserves_wf — remove preserves wf when capacity coherent
  158. step_preserves_well_formed — HEADLINE: combined invariant (consistent ∧ coherent ∧ NoDup) preserved
  159. well_formed_exec — HEADLINE: well_formed preserved through non-Place traces

  -- Permutation invariance (Cartier-Foata theorem for the store) --
  170. object_op_det — object_op target is deterministic (same label → same oid)
  171. pd_ao_NoDup — pairwise_different + all_object_ops implies NoDup on labels
  172. non_targeting_preserves_find_backward — CORE: backward frame (post→pre find preservation)
  173. object_op_swap_executable — executability transfer for independent object ops
  174. permutation_invariance — HEADLINE: any permutation of independent ops gives same result

  -- Multi-segment independence (cross-segment safety) --
  175. cross_segment_frame — CORE: ops on segment i preserve segment j (i ≠ j)
  176. global_step_preserves_wf — HEADLINE: global well-formedness is invariant
  177. global_step_preserves_consistency — HEADLINE: global consistency is invariant
  178. cross_segment_commute — HEADLINE: ops on different segments commute unconditionally
  179. global_step_preserves_length — segment count preserved by all operations

  -- Abstract permutation invariance (Cartier-Foata for any LTS) --
  180. abstract_permutation_invariance — HEADLINE: any perm of pairwise-independent ops gives same result (2 axioms)
  181. pairwise_indep_NoDup — irreflexivity + pairwise independence implies NoDup
  182. pairwise_indep_perm — pairwise independence transfers through Permutation
  183. mooncake_permutation — HEADLINE: Mooncake as one-line instance of the abstract theorem
  184. exec_to_aexec — bridge: Mooncake exec → abstract aexec
  185. aexec_to_exec — bridge: abstract aexec → Mooncake exec

  -- Global safety capstone (definitive safety characterization) --
  160. serving_safety — HEADLINE: read-in-progress objects survive arbitrary non-targeting traces
  161. serving_path — HEADLINE: COMPLETE objects are constructively readable
  162. reads_are_transparent — HEADLINE: balanced read sessions don't change state
  163. balanced_read_preserves_wf — corollary: balanced reads preserve well-formedness
  164. drain_respects_readers — HEADLINE: drain lifecycle preserves active readers
  165. batch_eviction_safe — HEADLINE: batch eviction of unrelated objects preserves reads
  166. invariant_preservation — HEADLINE: well_formed is invariant (combines consistency + coherence + NoDup)
  167. consistency_always_preserved — HEADLINE: store_consistent invariant (no freshness needed)
  168. protection_dichotomy — HEADLINE: every object is protected xor evictable
  169. refcount_is_read_balance — HEADLINE: refcount = #BeginRead - #EndRead (conservation law)

KNOWN GAPS
==========

  - Eviction watermark policy: source triggers eviction when usage exceeds
    high_watermark_ratio (default 0.95).  We model eviction protection but
    not the trigger condition.
    STATUS: deferred.  REASON: requires rational arithmetic or fixed-point
    encoding; safety properties don't depend on the threshold value.

  - Lease-based pinning: source uses TTL leases (etcd) to temporarily pin
    objects.  We model pin levels but not time-based expiry.
    STATUS: deferred.  REASON: temporal properties need a clock model;
    the structural invariant (pinned → protected) holds regardless.

  - Concurrent segment access: source uses ScopedSegmentAccess with mutexes
    and atomic refcounts.  We model the logical invariants, not the locking.
    STATUS: abandoned.  REASON: Coq is not the right tool for lock-level
    verification; the value is in protocol composition, not mutex correctness.

  - Standby/HA promotion protocol: source has a 10-state machine
    (STOPPED→CONNECTING→SYNCING→WATCHING→PROMOTING→PROMOTED + error states).
    STATUS: deferred.  REASON: complex but orthogonal to KV cache correctness.
    Could be its own module if someone wants to formalize HA failover.

  - Replica variant types: source distinguishes MEMORY, DISK, LOCAL_DISK
    replicas with different descriptors (MemoryDescriptor, DiskDescriptor).
    We use a single ReplicaStatus without type discrimination.
    STATUS: deferred.  REASON: the lifecycle state machine is the same for
    all replica types; the type affects transport selection, not safety.

  - P2P shard replication: the Go-based p2p-store tracks Gold vs Replica
    locations per shard with etcd metadata.  This is a distribution protocol
    with its own consistency requirements.
    STATUS: deferred.  REASON: separate language (Go), separate concern
    (distribution vs lifecycle), would need its own formalization.

  - drain_wf (well-foundedness of drain_step relation): drain_step wraps
    LRemove, but remove_object is always enabled as a no-op filter.
    Removing a non-existent object doesn't decrease any measure.
    STATUS: abandoned.  REASON: rank_wf requires strict decrease on every
    step.  The useful property (drain terminates after bounded removals)
    is captured by drain_bounded without needing well-foundedness.

  - Transfer operations in the LTS: the plan called for 9 store labels
    including LBeginTransfer, LCompleteSlice, LFinishTransfer,
    LPromoteReplica.  Only 5 labels were implemented.
    STATUS: deferred.  REASON: transfer status changes don't interact
    with segment operations in the current model.  Adding them would
    increase case analysis in every theorem without enabling new
    cross-cutting results.  Can be added when transfer-segment coupling
    properties are needed.

  - Replica refinement morphism: StoreRefinement.v was planned to include
    both segment and replica projections.  Only segment refinement was
    implemented.
    STATUS: deferred.  REASON: replica projection requires a find-by-id
    function and handling of absent objects (project to RsRemoved).
    Segment refinement was higher priority since it enables drain
    monotonicity.  Can be added when per-object trace reasoning is needed.

Layer structure:
  theories/Core/          — Foundation types and state machines (Layer 1)
  theories/Store/         — Segment management and drain protocol (Layer 2-3)
  theories/Transfer/      — Transfer protocol and replica bridge (Layer 2-3)
  theories/Composition/   — Cross-module safety composition (Layer 4)
  theories/LTS/           — Labeled transition system, acyclicity, refinement, liveness (Layer 2-4)

Shared libraries:
  lib/  — distributed_ml_coq (git submodule, build with make -C lib/<name>)

Source alignment:
  Source: data_source/Mooncake
  Config: .source-alignment.yml
  Run `rocq-alignment` for coverage report
  Run `rocq-alignment --check-drift` before pulling source updates

  Key source files mapped:
    mooncake-store/include/replica.h     → Core/Core.v (ReplicaStatus)
    mooncake-store/include/segment.h     → Core/Core.v (SegmentStatus)
    mooncake-store/include/eviction_strategy.h → Core/Core.v (PinLevel, eviction_protected)
    mooncake-store/include/transfer_task.h → Core/Core.v (TransferTask, OpCode)
    mooncake-store/include/types.h       → Core/Core.v (ObjectId, SegmentId, NodeId)
    mooncake-store/src/store.cpp         → Store/SegmentManager.v (Segment ops)
    mooncake-transfer-engine/include/transport/transport.h → Transfer/TransferProtocol.v

Quality gates (rocq-quality):
  - All proofs complete (no Admitted)
  - Layer discipline (lower layers don't import higher)
  - rocq-diagnose gate style --strict

FORMALIZATION MAP
=================

Source module -> Coq module -> What and why.

  mooncake-store (replica.h, segment.h, types.h, eviction_strategy.h)
    → Core/Core.v
    FIDELITY: specification
    WHAT: Replica lifecycle (5-state machine), segment health (4-state
    machine), pin levels with ordering, object metadata with eviction
    protection predicate, transfer task with slice counting.
    WHY: These are the protocol-level types that all higher modules
    depend on.  Getting the state machines right prevents composition bugs.
    KEY RESULTS: replica_no_self_transition, no_transition_from_removed,
    processing_not_evictable, hard_pinned_protected, active_readers_protected

  mooncake-store (store.cpp, allocator.h)
    → Store/SegmentManager.v
    FIDELITY: specification
    WHAT: Segment bookkeeping (capacity tracking), object placement
    and removal, segment status transitions, drain/unmount safety
    preconditions.
    WHY: Placement on wrong-status segments and premature unmounting
    are the primary segment-level safety violations.
    KEY RESULTS: place_requires_ok, empty_drained_safe, place_preserves_status

  mooncake-transfer-engine (transport.h, transfer_task.h)
    → Transfer/TransferProtocol.v
    FIDELITY: specification
    WHAT: Transfer task state machine (4-state), slice counting with
    monotonicity invariant, completion coherence, task lifecycle operations.
    WHY: Transfer completion must be monotonic (slices only increase)
    and coherent (completed iff all slices done).  Terminal states must
    be absorbing.
    KEY RESULTS: create_task_slice_inv, complete_slice_preserves_inv,
    transfer_no_self_transition, task_finished_iff

  mooncake-store (segment.h drain logic, PrepareUnmount, CommitUnmount)
    → Store/DrainProtocol.v
    FIDELITY: specification
    WHAT: Two-phase drain/unmount protocol.  DRAINING blocks allocation,
    objects must be explicitly removed, DRAINED + empty = safe to unmount.
    WHY: Premature unmounting loses data.  The drain protocol is the
    primary defense; it must be proven correct as a sequence.
    KEY RESULTS: drain_sequence_valid, drain_sequence_blocks_placement,
    drain_then_unmount_safe, drainable_no_processing

  mooncake-store (replica.h mark_complete, transfer_task.h callback)
    → Transfer/ReplicaTransfer.v
    FIDELITY: specification
    WHAT: Bridge between transfer completion and replica status.
    task_succeeded → PROCESSING → COMPLETE.  task_failed → PROCESSING →
    FAILED.  REMOVED replicas cannot have active transfers.
    WHY: This is the critical coupling between transfer and replica
    state machines.  An orphaned transfer or premature promotion is
    a data integrity bug.
    KEY RESULTS: transfer_success_valid_transition,
    transfer_failure_valid_transition, removed_not_coupled_active

  composition of replica.h × segment.h × transfer_task.h
    → Composition/StoreComposition.v
    FIDELITY: specification
    WHAT: Composed global invariant (store_consistent) over segment +
    transfers.  Four protocol gates hold simultaneously.  Placement,
    removal, and drain operations preserve consistency.
    WHY: The capstone — this is what the formalization exists to prove.
    Individual state machines are trivial; their composition is where
    bugs hide.
    KEY RESULTS: four_gates_hold, evictable_characterization,
    processing_only_on_ok, place_preserves_consistency,
    remove_preserves_consistency, begin_drain_preserves_consistency,
    no_incomplete_reads, no_unmount_with_objects

  composition of replica.h × segment.h × transfer_task.h
    → LTS/StoreLTS.v
    FIDELITY: specification
    WHAT: Unified labeled transition system.  5 labels (LPlace, LRemove,
    LBeginDrain, LCompleteDrain, LUnmount).  store_step wraps existing
    operations with precondition guards.  Multi-step preservation via DML
    lstar_preserved.
    WHY: Enables multi-step reasoning about arbitrary execution traces.
    Single-step preservation is necessary but not sufficient for safety
    under interleaving.
    KEY RESULTS: store_step_preserves_consistency, store_consistent_star,
    store_step_segment_status

  composition of replica.h × segment.h × transfer_task.h
    → LTS/Acyclicity.v
    FIDELITY: specification
    WHAT: Rank functions for all three component state machines.  Global
    acyclicity via DML rank_acyclic.  Well-foundedness via DML rank_wf.
    Composed store_rank with progress step analysis.
    WHY: Acyclicity subsumes the single-step no-self-transition lemmas
    (proves the stronger property: no state reaches itself via any path
    length).  Well-foundedness guarantees convergence without external input.
    KEY RESULTS: replica_acyclic, segment_acyclic, transfer_acyclic,
    replica_wf, segment_wf, transfer_wf, progress_step_rank_nonincreasing,
    drain_step_decreases_rank

  composition of replica.h × segment.h
    → LTS/StoreRefinement.v
    FIDELITY: specification
    WHAT: Refinement morphism projecting store state to segment component.
    Invariant lifting and trace projection via DML Refinement module.
    Segment monotonicity: status never regresses.
    WHY: Enables reasoning about component-level properties from composed
    traces.  Monotonicity proves that drain is irreversible: once draining
    begins, the segment can never accept new objects.
    KEY RESULTS: segment_invariant_lift, segment_trace_projection,
    once_draining_never_ok, once_draining_no_placement,
    segment_rank_monotone

  mooncake-store/include/segment.h (drain lifecycle)
    → LTS/DrainProgress.v
    FIDELITY: specification
    WHAT: Drain termination as a liveness property.  Object count is a
    Lyapunov function: bounded below by 0, never increases on draining
    segments, strictly decreases on removal of present objects.
    WHY: First liveness property in the formalization.  Safety says "bad
    things don't happen"; liveness says "good things eventually happen".
    Drain termination means a segment can eventually be reclaimed.
    KEY RESULTS: drain_never_adds_objects, remove_present_decreases,
    drain_completion_enabled, drain_trace_monotone, drain_invariant,
    drain_bounded

  mooncake-store/include/segment.h (full lifecycle)
    → LTS/Convergence.v
    FIDELITY: specification
    WHAT: Tight quantitative lifecycle bound.  Constructs the explicit
    optimal trace [BeginDrain, Remove₁…Removeₙ, CompleteDrain, Unmount]
    and proves it reaches SegUnmounting in exactly n+3 steps.  Also
    proves removal commutativity (order doesn't matter).
    WHY: Quantitative result — not just "the system converges" but
    "it converges in exactly this many steps via this specific trace."
    The commutativity result reveals inherent parallelism in the drain
    phase: all n removals are independent and could execute concurrently.
    KEY RESULTS: filter_comm, remove_objects_comm, sequential_remove_lstar,
    lifecycle_convergence, lifecycle_trace_length, segment_reclaimable

  mooncake-store/include/replica.h (inc_refcnt, dec_refcnt, fn_is_busy)
    → Store/RefcountProtocol.v
    FIDELITY: specification
    WHAT: Reference counting operations on objects and segments.  Object
    lookup by ID, in-place metadata update, refcount increment/decrement.
    Connection between refcount and eviction protection predicate.
    WHY: The refcount mechanism is the primary safety mechanism in
    Mooncake — it prevents eviction of objects being read via RDMA.
    Formalizing the operations and their properties is prerequisite for
    the eviction safety theorem.
    KEY RESULTS: inc_protects, evictable_zero_refcnt, evictable_is_complete,
    inc_segment_refcnt_protects, find_after_update, update_preserves_live

  mooncake-store/include/replica.h + src/master_service.cpp
    → LTS/EvictionSafety.v
    FIDELITY: specification
    WHAT: Enriched LTS with 8 operations (original 5 + BeginRead, EndRead,
    Evict).  BeginRead increments refcount on COMPLETE objects.  EndRead
    decrements.  Evict requires eviction_protected = false.  Key safety
    theorem: a read in progress (refcnt > 0) makes eviction impossible.
    WHY: This is the essence of Mooncake's safety.  The refcount protocol
    creates a separation between "active read" and "eligible for eviction"
    that cannot be violated by any interleaving of operations.
    KEY RESULTS: read_eviction_safety, begin_read_blocks_eviction,
    evict_is_guarded_remove, triple_protection, eviction_requires_all_absent

  mooncake-store/src/master_service.cpp (BatchEvict, concurrent reads)
    → LTS/ProtectionFrame.v
    FIDELITY: specification
    WHAT: Frame properties for concurrent multi-object operations.
    Operations targeting one object (remove, inc_refcnt, dec_refcnt,
    evict, read) do not affect another object's metadata or findability.
    General frame theorem (non_targeting_preserves_find) plus headline
    concurrent safety: evicting B doesn't affect A's refcount protection,
    reading B doesn't interfere with A's protection, drain removals
    respect active reads on other objects.
    WHY: The single-object safety (EvictionSafety.v) proves protection
    per object; the frame properties extend this to multi-object
    concurrent scenarios.  This is the bridge between per-object
    invariants and system-wide safety under arbitrary interleavings.
    KEY RESULTS: non_targeting_preserves_find, concurrent_eviction_safe,
    concurrent_reads_independent, drain_preserves_read_protection,
    end_read_preserves_other_protection, trace_non_targeting_preserves_find,
    trace_read_protection

  mooncake-store/src/master_service.cpp (AddReplica, BatchEvict)
    → LTS/ObjectLifecycle.v
    FIDELITY: specification
    WHAT: Constructive completeness proofs for object operations.
    Placement makes objects findable, eviction makes them unfindable.
    Protection dichotomy: every findable object is either protected
    (eviction impossible) or unprotected (eviction possible), never
    both.  BeginRead constructively changes protection status.
    WHY: Complements the safety/frame properties (soundness) with
    completeness: operations achieve their intended effects.  Together,
    soundness + completeness characterize the protocol exactly.
    KEY RESULTS: eviction_makes_unfindable, place_makes_findable,
    read_or_evict, eviction_possible, read_possible,
    begin_read_protects, evict_and_remove_same_effect

  mooncake-store/src/master_service.cpp (concurrent operations)
    → LTS/Diamond.v
    FIDELITY: specification
    WHAT: Diamond property for independent operations.  Operations
    targeting different objects commute: executing l1 then l2 reaches
    the same state as l2 then l1.  Reveals Mooncake's store has a
    partially commutative monoid of operations — scheduling order does
    not affect the final state for independent operations.
    WHY: The frame properties (ProtectionFrame.v) show non-interference
    at the level of predicates; the diamond property strengthens this
    to full state equality.  This is the Church-Rosser property for
    the concurrent object store.
    KEY RESULTS: general_object_diamond (capstone),
    concurrent_reads_diamond, evict_read_diamond,
    remove_remove_diamond, evict_evict_diamond, remove_read_diamond,
    end_read_end_read_diamond, begin_read_end_read_diamond,
    evict_end_read_diamond, remove_end_read_diamond,
    drain_read_diamond, drain_evict_diamond,
    remove_update_seg_comm, update_update_seg_comm, remove_remove_seg_comm,
    read_session_transparent, read_session_store_transparent

  mooncake-store/src/master_service.cpp (object ID management)
    → LTS/NoDupPreservation.v
    FIDELITY: specification
    WHAT: Proves that NoDup (unique object IDs) is preserved by every
    operation in the enriched LTS.  Closes the gap where many theorems
    assumed NoDup as a hypothesis without proving it was maintained.
    WHY: NoDup is the structural invariant that makes object identity
    well-defined.  Without it, find_object could return multiple matches
    and update_object_meta could modify unintended objects.
    KEY RESULTS: nodup_step_preserved (headline), nodup_filter_preserved,
    nodup_map_id_preserved, nodup_remove_preserved, nodup_update_preserved

  mooncake-store/src/master_service.cpp (concurrent operations)
    → LTS/TraceEquivalence.v
    FIDELITY: specification
    WHAT: Mazurkiewicz trace monoid structure for the enriched LTS.
    Defines multi-step execution (exec), label independence, and proves
    the permutation theorem: swapping adjacent independent operations
    in a trace preserves the final state.  Unifies all 40 individual
    diamond theorems (16 object×object + 24 status×object) into a
    single parametric independent_diamond, then lifts it to traces.
    WHY: The diamond property (Diamond.v) proves commutativity for
    individual operation pairs.  The permutation theorem shows this
    structure extends to arbitrary traces — scheduling order is
    irrelevant for independent operations.  This is the algebraic
    capstone: the store is a partially commutative monoid.
    KEY RESULTS: independent_diamond (unified diamond),
    swap_preserves_result (permutation theorem), exec_deterministic,
    exec_split, exec_join, independent_sym

  mooncake-store/include/replica.h (inc_refcnt, dec_refcnt)
    → LTS/RefcountCounting.v
    FIDELITY: specification
    WHAT: Refcount conservation law across multi-step traces.  Defines
    count_begins/count_ends (trace-level read event counting) and proves
    the exact accounting identity: refcount_final + #EndRead =
    refcount_initial + #BeginRead.  Corollary: excess BeginReads over
    EndReads guarantee eviction protection.
    WHY: The single-step read_eviction_safety (EvictionSafety.v) says
    "refcount > 0 blocks eviction" but doesn't connect refcount to the
    trace history.  This theorem makes the connection quantitative:
    refcount is exactly the difference between begins and ends.
    Protection becomes a consequence of arithmetic, not case analysis.
    KEY RESULTS: refcount_counting (conservation law),
    protection_from_counting (safety corollary), begin_read_find,
    end_read_find, find_after_update_seg

  mooncake-store/src/master_service.cpp (operation dispatch)
    → LTS/Denotational.v
    FIDELITY: specification
    WHAT: Computable evaluation function for the enriched LTS.  Defines
    step_eval (single-step, returns option StoreState) and eval (multi-step
    fold).  Proves adequacy: eval ls st = Some st' ↔ exec st ls st'.
    Proves compositionality: eval distributes over trace concatenation.
    Includes boolean decision procedures for no_in_progress and
    safe_to_unmount with reflection lemmas.
    WHY: Bridges the operational (relational, good for proofs) and
    denotational (functional, good for computation) views.  The adequacy
    theorem licenses using eval for testing and extraction while keeping
    exec for inductive proofs.  Compositionality reveals eval as a monoid
    homomorphism from list concatenation to Kleisli composition.
    KEY RESULTS: eval_adequacy (headline), eval_app (compositionality),
    step_eval_sound, step_eval_complete, no_in_progress_reflect,
    safe_to_unmount_reflect

  mooncake-store/src/master_service.cpp (drain + concurrent reads)
    → LTS/DrainSafety.v
    FIDELITY: specification
    WHAT: Composition of frame properties, eviction safety, NoDup
    preservation, and denotational semantics.  Bridges exec and lstar
    relations.  Proves that drain lifecycle respects active readers:
    removing non-read objects preserves actively-read objects' metadata
    and eviction protection.  Connects to eval for computability.
    WHY: This is the capstone composition answering the key operational
    question: "Can Mooncake drain a segment while reads are in progress?"
    Yes — as long as only non-read objects are removed.  Composes
    ProtectionFrame + EvictionSafety + NoDupPreservation + Denotational.
    KEY RESULTS: drain_phase_safety (headline), drain_phase_eval
    (denotational connection), full_drain_safety (general trace safety),
    removal_trace_preserves_find, nodup_removal_trace,
    exec_to_lstar, lstar_to_exec

  mooncake-store/src/master_service.cpp (all operations)
    → LTS/RichConsistency.v
    FIDELITY: specification
    WHAT: Extends store_consistent preservation from the 5-label store_step
    to the full 8-label rich_store_step.  The key factoring insight:
    meta_update_preserves_consistency — any metadata update that preserves
    om_status also preserves store_consistent.  This covers both BeginRead
    (inc_refcnt) and EndRead (dec_refcnt).  Evict reuses the Remove proof.
    Multi-step and denotational corollaries follow.
    WHY: The original store_step_preserves_consistency covers only 5
    operations.  The enriched LTS adds BeginRead, EndRead, Evict — the
    three operations critical to concurrent read safety.  This theorem
    closes the gap: the global invariant holds through ALL operations.
    KEY RESULTS: rich_step_preserves_consistency (headline),
    rich_consistent_exec (multi-step), eval_preserves_consistency
    (denotational), meta_update_preserves_consistency (factoring lemma)

  mooncake-store/src/master_service.cpp (all operations)
    → LTS/GlobalSafety.v
    FIDELITY: specification
    WHAT: Capstone safety theorems answering the four questions a system
    designer cares about.  Q1: read-in-progress objects cannot be evicted
    (serving_safety).  Q2: drain lifecycle respects active readers
    (drain_respects_readers).  Q3: balanced reads don't change state
    (reads_are_transparent).  Q4: global invariant maintained through
    all operations (invariant_preservation).  Also: protection dichotomy,
    batch eviction safety, refcount conservation law.
    WHY: The definitive statement of what the formalization proves.
    Individual theorems live in their respective modules; this file
    restates them as the executive summary with unified documentation.
    KEY RESULTS: serving_safety, serving_path, reads_are_transparent,
    drain_respects_readers, batch_eviction_safe, invariant_preservation,
    protection_dichotomy, refcount_is_read_balance

  mooncake-store/src/master_service.cpp (concurrent operations)
    → LTS/PermutationInvariance.v
    FIDELITY: specification
    WHAT: Full permutation invariance for independent operations — the
    Cartier-Foata theorem for the Mooncake store.  Any permutation of
    pairwise-independent object operations reaches the same final state.
    Proves the backward frame (post→pre find preservation), executability
    transfer (if l1;l2 works and they're independent, so does l2;l1),
    and the main theorem by induction on Coq's Permutation derivation.
    Includes NoDup derivation from pairwise_different + all_object_ops
    (via object_op determinism).
    WHY: The swap_preserves_result theorem (TraceEquivalence.v) only
    handles adjacent transpositions.  This theorem extends to arbitrary
    permutations: scheduling order is completely irrelevant for independent
    operations.  This is the strongest commutativity result — it says not
    just "the result is the same if both orderings work" but "if one
    ordering works, ALL orderings work and give the same result."
    KEY RESULTS: permutation_invariance (headline),
    non_targeting_preserves_find_backward (backward frame),
    object_op_swap_executable (executability transfer),
    object_op_det (determinism), pd_ao_NoDup (NoDup from pairwise_different)

  mooncake-store/src/store.cpp (segment_map, per-segment ops)
    → LTS/MultiSegment.v
    FIDELITY: specification
    WHAT: Multi-segment model extending the formalization from single-segment
    to a collection of independent segment stores.  Defines GlobalStore
    (list of StoreState), GlobalLabel (segment index + operation), and
    global_step (apply operation to specified segment).  Proves cross-segment
    independence: operations on different segments don't affect each other.
    Cross-segment commutativity: operations on different segments commute
    unconditionally (stronger than label-level independence — structural
    commutativity by construction).  Per-segment properties lift to global:
    well-formedness and consistency are preserved by all global steps.
    WHY: Mooncake is a DISTRIBUTED store — objects live on different
    segments across different nodes.  The single-segment model captures
    per-segment safety, but the real system manages many segments.  This
    module proves that per-segment properties compose: the global store
    is safe because each segment is independently safe and operations on
    different segments don't interact.
    KEY RESULTS: cross_segment_frame, cross_segment_commute,
    global_step_preserves_wf, global_step_preserves_consistency,
    global_step_preserves_length

  (abstract — no source correspondence)
    → LTS/AbstractCommutation.v
    FIDELITY: specification
    WHAT: Abstract algebraic framework extracting the minimal axioms for
    permutation invariance.  Given ANY labeled transition system with an
    independence relation satisfying just TWO axioms — (I) irreflexivity:
    no label is independent of itself, (II) swap: independent operations
    can be executed in either order with the same result — full permutation
    invariance holds for pairwise-independent traces.  Mooncake is proved
    as a one-line instance.  Includes bridge lemmas between abstract aexec
    and Mooncake's concrete exec.
    WHY: Separates the mathematical structure from the domain.  The abstract
    theorem reveals that permutation invariance is not about Mooncake — it's
    about ANY concurrent system with independent operations.  The proof
    factors into three steps: (1) irreflexivity + pairwise independence =>
    NoDup, (2) NoDup transfers through Permutation, (3) induction on
    Permutation using Swap.  Symmetry of independence is NOT needed.
    KEY RESULTS: abstract_permutation_invariance (headline),
    mooncake_permutation (instance), pairwise_indep_NoDup,
    pairwise_indep_perm, exec_to_aexec, aexec_to_exec

  mooncake-store/src/store.cpp (allocation path) + allocator.h
    → LTS/CapacityCoherence.v
    FIDELITY: specification
    WHAT: Proves capacity bookkeeping invariant: seg_used faithfully
    tracks the total size of objects on the segment.  Core arithmetic:
    partition identity (total = kept + freed) for the remove operation.
    Place preserves coherence (adds om_size to both fields).  Remove
    preserves coherence (subtracts freed from both).  Metadata updates
    (inc/dec_refcnt) preserve coherence because they preserve om_size.
    Combined invariant: store_well_formed = consistent ∧ coherent ∧ NoDup,
    preserved by every step (with fresh IDs for Place).
    WHY: Resource correctness — the store must not lose track of space.
    The capacity invariant ensures that used ≤ capacity is maintained
    as long as has_room is checked before placement.  This is the
    accounting layer beneath the safety properties.
    KEY RESULTS: step_preserves_coherence (headline),
    step_preserves_well_formed (combined invariant),
    well_formed_exec (multi-step), sum_sizes_partition (core arithmetic),
    place_preserves_wf, remove_preserves_wf

NEXT STEPS
==========

  DONE:
  - StoreComposition, ReplicaTransfer, DrainProtocol (original plan items 1-3)
  - StoreLTS, Acyclicity, StoreRefinement, DrainProgress (LTS plan items 1-4)
  - Convergence (tight lifecycle bound, Tao-inspired)
  - RefcountProtocol (refcount operations and protection proofs)
  - EvictionSafety (enriched LTS with read/evict safety)
  - ProtectionFrame (concurrent safety frame properties)
  - ObjectLifecycle (constructive completeness and protection dichotomy)
  - Diamond (commutativity of independent operations, Church-Rosser, read transparency)
  - NoDupPreservation (unique object IDs preserved through all operations)
  - TraceEquivalence (Mazurkiewicz trace monoid, permutation theorem)
  - RefcountCounting (refcount conservation law, trace-level protection)
  - Denotational (computable eval function, adequacy theorem, compositionality)
  - DrainSafety (drain lifecycle respects active readers, exec↔lstar bridge)
  - RichConsistency (store_consistent through all 8 enriched operations)
  - CapacityCoherence (resource accounting invariant, combined well-formedness)
  - GlobalSafety (capstone: definitive safety characterization)
  - PermutationInvariance (Cartier-Foata theorem: any permutation of independent ops gives same state)
  - MultiSegment (cross-segment independence: per-segment properties compose globally)
  - AbstractCommutation (abstract algebraic framework: 2 axioms → full permutation invariance)

  Remaining extensions:

  1. (DONE — see CapacityCoherence.v)

  2. (DONE — see MultiSegment.v: cross-segment frame, commutativity, invariant lifting)

  3. Transfer-segment coupling — prove that completing a transfer on a
     draining segment preserves drain_consistent.  Requires adding
     transfer labels to the enriched LTS.
     Source: transfer_task.h callback + segment.h drain logic.

  4. (DONE — see DrainSafety.v: drain_phase_safety, full_drain_safety)
